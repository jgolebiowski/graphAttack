language: python
python:
  - "3.5"
  - "3.6"

# command to install dependencies
install:
  - python3 -m pip install pytest
  - python3 -m pip install -r requirements.txt
# command to run tests
script:
  - python3 -m pytest tests.py # or py.test for Python versions 3.5 and below